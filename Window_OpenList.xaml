<Window x:Class="Task_list_app.Window_OpenList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task_list_app"
        mc:Ignorable="d"
        Title="" Height="450" Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>

        <Canvas Grid.Row="0" Style="{StaticResource HeadersBackground}"/>
        <Canvas Grid.Row="1" Style="{StaticResource WorkZoneBackground}"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel Margin="10">
                
            </StackPanel>
        </ScrollViewer>
        <TextBlock Text="History list" Style="{StaticResource HeadersText}"/>
        
        
    </Grid>
</Window>
